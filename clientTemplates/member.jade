.well.member
  .userImage
    img(src= locals.member.picUrl() + '?s=50')
  h2= locals.member.fullName()
  p.activeTask= (locals.activeTaskTitle || '')
  p.healthPerc= (locals.healthPerc || 0)
  p.attackPerc= (locals.attackPerc || 0)